import { ButtonBase } from '@material-ui/core'

const EditorHeaderMenuBarButton = (props) => {
  return <ButtonBase className="text-sm py-0.5 px-2 rounded-sm hover:bg-black hover:bg-opacity-5" {...props} />
}

export default EditorHeaderMenuBarButton
